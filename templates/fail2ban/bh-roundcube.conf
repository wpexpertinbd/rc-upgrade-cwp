# /etc/fail2ban/filter.d/bh-roundcube.conf
# Matches Roundcube failed-login lines (needs $config['log_logins'] = true and
# real client IP via the harden phase, else <HOST> is just 127.0.0.1).
[Definition]
failregex = (?:Failed login for|IMAP Error: Login failed for) .*? from <HOST>
ignoreregex =
